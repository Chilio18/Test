version: '3.9'

# Override for local development — only infrastructure, run API+frontend locally
services:
  postgres:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"

  minio:
    ports:
      - "9000:9000"
      - "9001:9001"

  seq:
    ports:
      - "5341:80"
